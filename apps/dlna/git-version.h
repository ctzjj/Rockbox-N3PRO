/* git-version.h - substitute for the upstream build-time version file
 * (upstream generates this from the git tree; we vendor a release). */
#ifndef _GIT_VERSION_H
#define _GIT_VERSION_H
#define GM_COMPILE_VERSION "0.0.8"
#endif
